name: Keep Render Alive

on:
  schedule:
    - cron: '*/5 * * * *'  # 每 5 分钟 ping 一次

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render backend
        run: |
          curl -s -o /dev/null -w "%{http_code}" https://talking-platform.onrender.com/api
